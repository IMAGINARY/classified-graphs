/** List of proposed challenges as target graphs
 *
 * To configure the list, just fill the list with the identifiers of the graphs you want.
 * The identifier is the "file" field that you can find in the file ../graph-gallery/graphs-list.json
 *
 * There is always a "Random" option in the targets list, that chooses randomly from the graph list.
 */

const targetChoices = ['complete_4', 'trivalent_5_70'];
export default targetChoices;
